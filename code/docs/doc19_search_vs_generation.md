# 检索与生成的分工

在 RAG 中，检索和生成是两条不同但互相依赖的链路。检索阶段负责找最相关的资料，生成阶段负责把这些资料组织成自然语言答案。

如果检索阶段找错了，生成阶段通常也救不回来；如果检索阶段找对了，但 Prompt 组织不好，生成阶段依然可能表现不稳。因此两边都不能只看其中一边。

很多系统效果不佳时，开发者会第一时间怀疑“大模型不够强”，但真正的问题往往出在前面的检索设计上。
