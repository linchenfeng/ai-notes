# 日志与可观测性

当 AI 接口开始进入真实联调阶段，日志就不再是可有可无的附加项。没有日志时，你很难知道一次请求为什么失败、耗时在哪、错误发生在哪个阶段。

一个基础日志体系通常至少会记录：请求开始、请求结束、耗时、请求 ID、错误信息，以及关键输入输出指标。对后续 RAG 来说，还可以逐步增加检索到的 chunk 数量、top-k、拒答触发等信息。

可观测性做得越早，后面排查 bad case 和性能问题就越省力。
