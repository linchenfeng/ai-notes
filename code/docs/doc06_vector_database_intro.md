# 向量数据库入门

向量数据库可以理解成一种专门用来存储向量并支持相似度检索的系统。它的核心价值不在“保存数据”本身，而在“当用户提问时，可以更快地找出最相关的向量”。

在小型实验里，也可以不一开始就上完整向量数据库，而是先用本地文件或内存索引跑通最小闭环。对初学者来说，更重要的是先理解索引和相似度检索的基本原理。

当文档规模增大，或者希望支持更稳定的查询和 metadata 过滤时，向量数据库的价值才会变得更明显。
