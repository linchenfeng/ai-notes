# Retrieval 与 Top-K

检索阶段的任务是根据用户问题，从知识库里找出最相关的若干个 chunk。常见做法是取相似度最高的前 K 条，也就是 top-k。

K 不是越大越好。K 太小，可能漏掉关键证据；K 太大，又可能把无关内容一起塞进生成阶段，导致答案变得冗长或失焦。

因此 top-k 的选择通常需要结合实际测试问题来调。很多 RAG 效果不好的问题，不一定是模型不会答，而可能是检索召回的内容就不够好。
