# 文档 Metadata

Metadata 可以理解成附着在文档或 chunk 上的结构化信息，例如标题、来源、标签、文件路径、章节、创建时间或主题分类。

在早期实验里，很多人容易忽略 metadata，只保留纯文本。但一旦要做引用来源、结果过滤、多主题知识库或更清晰的前端展示时，metadata 的价值会立刻显现出来。

一个设计合理的知识库，不只是存文本，还会尽量保留文档的上下文身份信息。
